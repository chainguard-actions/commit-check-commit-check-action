name: Test commit-check dry-run pull request
on:
  workflow_dispatch:
jobs:
  test:
    runs-on: ubuntu-latest
    env:
      GITHUB_REF: refs/pull/1/merge
      GITHUB_EVENT_NAME: pull_request
      GITHUB_HEAD_REF: feat/new-feature
      GITHUB_BASE_REF: main
      GITHUB_TOKEN: ${{ github.token }}
      GH_TOKEN: ${{ github.token }}
      GITHUB_STEP_SUMMARY: /tmp/step-summary.md
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
      - name: Configure git for commit-check
        run: |
          git config --global user.email "pr-author@example.com"
          git config --global user.name "PR Author"
          # Make a test commit so there is a HEAD commit to check
          git commit --allow-empty -m "feat(scope): add pull request support"
      - name: Mock gh CLI to bypass attestation verification under act
        run: |
          cat > /usr/local/bin/gh << 'GHEOF'
          #!/bin/bash
          # Mock gh: always succeed for attestation verify
          exit 0
          GHEOF
          chmod +x /usr/local/bin/gh
      - id: action
        uses: .
        with:
          message: "true"
          branch: "true"
          author-name: "false"
          author-email: "false"
          dry-run: "true"
          job-summary: "false"
          pr-comments: "false"
      - name: Assert action completed without error
        run: |
          echo "Action completed successfully for pull request event in dry-run mode"
          echo "PASS"
