name: Release Drafter

on:
  push:
    branches:
      - "main"
  workflow_dispatch:

permissions:
  contents: write
  pull-requests: write

jobs:
  draft-release:
    uses: commit-check/.github/.github/workflows/release-drafter.yml@57e126f26f0e9efaf4de8960d60612b0756b7557 # main
